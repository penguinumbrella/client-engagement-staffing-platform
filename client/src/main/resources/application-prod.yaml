spring:
    config:
        activate:
            on-profile: prod

eureka:
    client:
        service-url:
            default-zone: http://client-platform-eureka-env.us-east-1.elasticbeanstalk.com/eureka/
    instance:
        prefer-ip-address: true
